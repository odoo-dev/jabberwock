export enum RelativePosition {
    BEFORE = 'BEFORE',
    AFTER = 'AFTER',
    INSIDE = 'INSIDE',
}

export enum Direction {
    BACKWARD = 'BACKWARD',
    FORWARD = 'FORWARD',
}

export const SELECTION_ANCHOR_CHAR = '[';
export const SELECTION_FOCUS_CHAR = ']';
