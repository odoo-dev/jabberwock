<templates>
    <!-- INSPECTOR -->
    <devtools-panel class="inspector" t-name="InspectorComponent" t-att-class="{active: props.isOpen}"
        t-on-node-selected="selectNode"
        t-on-keydown="onKeydown">
        <devtools-contents t-if="props.isOpen">
            <devtools-mainpane>
                <mainpane-contents>
                    <devtools-tree>
                        <TreeComponent
                            vNode="env.editor.vDocument.root"
                            isRoot="true"
                            selectedPath="state.selectedPath"
                            selectedID="state.selectedNode.id"/>
                    </devtools-tree>
                </mainpane-contents>
                <PathComponent
                    path="state.selectedPath"
                    selectedID="state.selectedNode.id"/>
            </devtools-mainpane>
            <devtools-sidepane>
                <InfoComponent vNode="state.selectedNode"/>
            </devtools-sidepane>
        </devtools-contents>
    </devtools-panel>

    <!-- INSPECTOR.Tree -->
    <devtools-node t-name="TreeComponent"
        t-att-class="{
            element: props.vNode.length and !props.vNode.value,
            folded: state.folded,
            root: props.isRoot,
            'self-closing': props.vNode.length == 0,
        }"
        tabindex="1"
        t-on-keydown="onKeydown"
        t-on-node-selected="selectNode">
        <t t-if="props.isRoot">
            <span class="element-name selectable-line"
                t-on-click="onClickNode" t-att-class="{
                    selected: props.selectedID == props.vNode.id,
                }">root</span>
            <t t-call="treeChildren"/>
        </t>
        <t t-else="">
            <span t-if="props.vNode.value" t-on-click="onClickNode"
                class="selectable-line" t-att-class="{
                    bold: props.vNode.format.bold,
                    italic: props.vNode.format.italic,
                    underlined: props.vNode.format.underlined,
                    selected: props.selectedID == props.vNode.id,
                }">
                <t t-esc="repr"/>
            </span>
            <t t-else="">
                <span class="element-name selectable-line" t-on-click="onClickNode"
                    t-att-class="{
                        bold: props.vNode.format.bold,
                        italic: props.vNode.format.italic,
                        underlined: props.vNode.format.underlined,
                        selected: props.selectedID == props.vNode.id,
                    }">
                    <t t-esc="repr"/>
                </span>
                <t t-call="treeChildren"/>
            </t>
        </t>
    </devtools-node>

    <t t-name="treeChildren">
        <div class="children">
            <TreeComponent t-if="!state.folded"
                t-foreach="props.vNode.children" t-as="child"
                t-key="child.id"
                vNode="child"
                selectedPath="props.selectedPath"
                selectedID="props.selectedID"/>
        </div>
    </t>

    <!-- INSPECTOR.Path -->
    <devtools-path t-name="PathComponent">
        <devtools-pathnode t-foreach="props.path" t-as="vNode"
            t-key="vNode.id"
            t-att-class="{
                selected: vNode.id == props.selectedID
            }"
            t-on-click="selectNode(vNode)">
            <t t-esc="getNodeRepr(vNode)"/>
        </devtools-pathnode>
    </devtools-path>

    <!-- INSPECTOR.Info -->
    <devtools-info t-name="InfoComponent">
        <div class="about">
            <span class="type">VNode</span> <t t-esc="props.vNode.type or &quot;?&quot;"/>
            <t t-if="props.vNode.value">:
                "<t t-esc="props.vNode.value"/>"
            </t>
            <span class="id"><t t-esc="props.vNode.id"/></span>
        </div>
        <div class="properties">
            <div class="divider">👤 About me</div>
            <table>
                <tbody>
                    <tr>
                        <td>id</td>
                        <td><t t-esc="props.vNode.id"/></td>
                    </tr>
                    <tr>
                        <td>type</td>
                        <td><t t-esc="props.vNode.type or &quot;UNKNOWN&quot;"/></td>
                    </tr>
                    <tr t-if="props.vNode.value">
                        <td>value</td>
                        <td>"<t t-esc="props.vNode.value"/>"</td>
                    </tr>
                    <tr>
                        <td>length</td>
                        <td><t t-esc="props.vNode.length"/></td>
                    </tr>
                    <tr>
                        <td>total length</td>
                        <td><t t-esc="props.vNode.totalLength()"/></td>
                    </tr>
                    <tr t-if="props.vNode.hasFormat()">
                        <td>format</td>
                        <td>
                            <ol style="list-style-type: none">
                                <li t-if="props.vNode.format.bold">Bold</li>
                                <li t-if="props.vNode.format.italic">Italic</li>
                                <li t-if="props.vNode.format.underlined">Underlined</li>
                            </ol>
                        </td>
                    </tr>
                    <tr>
                        <td>original tag</td>
                        <td><t t-esc="props.vNode.originalTag or &quot;None&quot;"/></td>
                    </tr>
                    <tr t-if="props.vNode.text().length">
                        <td>text</td>
                        <td>"<t t-esc="props.vNode.text()"/>"</td>
                    </tr>
                </tbody>
            </table>
            <div class="divider">👪 My Family</div>
            <table>
                <tbody>
                    <tr>
                        <td>index</td>
                        <td><t t-esc="props.vNode.index"/></td>
                    </tr>
                    <tr t-if="props.vNode.parent">
                        <td>parent</td>
                        <td t-on-click="selectNode(props.vNode.parent)">
                            <t t-esc="props.vNode.parent.type or &quot;?&quot;"/>
                        </td>
                    </tr>
                    <tr t-if="props.vNode.siblings.length > 1">
                        <td>siblings</td>
                        <td>
                            <ol style="list-style-type: none">
                                <li t-if="props.vNode.previousSibling"
                                    t-on-click="selectNode(props.vNode.previousSibling)">
                                    previous: <t t-esc="props.vNode.previousSibling.type or &quot;?&quot;"/>
                                </li>
                                <li t-if="props.vNode.nextSibling"
                                    t-on-click="selectNode(props.vNode.nextSibling)">
                                    next: <t t-esc="props.vNode.nextSibling.type or &quot;?&quot;"/>
                                </li>
                            </ol>
                        </td>
                    </tr>
                    <tr t-if="props.vNode.children.length">
                        <td>children</td>
                        <td>
                            <ol>
                                <li t-foreach="props.vNode.children"
                                    t-as="child" t-key="child.id"
                                    t-on-click="selectNode(child)">
                                    <t t-esc="child.type or &quot;?&quot;"/>
                                </li>
                            </ol>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </devtools-info>

    <!-- ACTIONS -->
    <devtools-panel class="actions" t-name="ActionsComponent"
        t-att-class="{active: props.isOpen}" tabindex="1"
        t-on-keydown="onKeydown">
        <devtools-contents t-if="props.isOpen">
            <t t-set="selectedRecord" t-value="state.registry[state.selectedActionIdentifier] or False"/>
            <devtools-mainpane style="overflow: hidden">
                <devtools-navbar>
                    <button t-on-click="openTab('selected')" t-att-class="{
                        selected: state.currentTab == 'selected',
                    }">Selected</button>
                    <button t-on-click="openTab('registry')" t-att-class="{
                        selected: state.currentTab == 'registry',
                    }">Registry</button>
                </devtools-navbar>
                <mainpane-contents t-if="state.currentTab == 'selected'" style="overflow: auto">
                    <table style="width: 100%;">
                        <tbody>
                            <tr t-foreach="state.actions" t-as="action"
                                t-key="action_index" class="selectable-line"
                                t-att-class="{
                                    selected: state.selectedActionIndex == action_index,
                                }"
                                t-on-click="selectAction(action_index)">
                                <td><t t-esc="action.type"/></td>
                                <td><t t-esc="action.name"/></td>
                            </tr>
                        </tbody>
                    </table>
                </mainpane-contents>
                <mainpane-contents t-elif="state.currentTab == 'registry'" style="overflow: auto">
                    <div t-foreach="Object.keys(state.registry)" t-as="recordID"
                        t-key="record_index" class="selectable-line"
                        t-att-class="{
                            selected: state.selectedActionIdentifier == recordID,
                        }"
                        t-on-click="selectRegistryRecord(recordID)">
                        <t t-esc="recordID"/>
                    </div>
                </mainpane-contents>
            </devtools-mainpane>
            <devtools-sidepane>
                <devtools-info t-if="state.currentTab == 'selected'">
                    <t t-set="selectedAction" t-value="state.actions[state.selectedActionIndex] or False"/>
                    <div class="about" t-if="selectedAction !== False">
                        <span class="type"><t t-esc="selectedAction.type"/></span> <t t-esc="selectedAction.name"/>
                    </div>
                    <div class="properties" t-if="selectedAction !== False">
                        <div class="divider">👤 About me</div>
                        <table>
                            <tbody>
                                <t t-foreach="Object.keys(selectedAction)" t-as="key">
                                    <tr t-if="key !== 'payload'" t-key="key_index">
                                        <td><t t-esc="key"/></td>
                                        <td><t t-esc="selectedAction[key]"/></td>
                                    </tr>
                                </t>
                            </tbody>
                        </table>
                        <t t-if="selectedAction and selectedAction.payload">
                            <t t-set="payload" t-value="selectedAction.payload"/>
                            <t t-call="actionPayload"/>
                        </t>
                        <t t-call="actionHandlers"/>
                        <t t-call="selectedActionHandler"/>
                    </div>
                </devtools-info>
                <devtools-info t-elif="state.currentTab == 'registry'">
                    <div class="about" t-if="selectedRecord !== False">
                        <t t-set="recordInfo" t-value="state.selectedActionIdentifier.split('.')"/>
                        <span class="type"><t t-esc="recordInfo[0]"/></span> <t t-esc="recordInfo[1]"/>
                    </div>
                    <div class="properties" t-if="selectedRecord !== False">
                        <t t-call="actionHandlers"/>
                        <t t-call="selectedActionHandler"/>
                    </div>
                </devtools-info>
            </devtools-sidepane>
        </devtools-contents>
    </devtools-panel>

    <!-- ACTIONS.Payload-->
    <t t-name="actionPayload">
        <div class="divider">💎 Payload</div>
        <table>
            <tbody>
                <tr t-foreach="Object.keys(payload)" t-as="key" t-key="key_index">
                    <t t-set="value" t-value="payload[key]"/>
                    <td>
                        <t t-esc="key"/>
                    </td>
                    <td>
                        <t t-if="value and key == 'domRangeChange'" t-call="payloadRange"/>
                        <t t-elif="value and key == 'vRangeLocation'" t-call="payloadVRange"/>
                        <t t-else=""><t t-esc="formatPayloadValue(value)"/></t>
                    </td>
                </tr>
            </tbody>
        </table>
    </t>

    <!-- ACTIONS.Payload.domRangeChange -->
    <t t-name="payloadRange">
        <table>
            <tbody>
                <tr t-foreach="Object.keys(value)" t-as="rangeKey" t-key="rangeKey_index">
                    <t t-set="value" t-value="value[rangeKey]"/>
                    <td>
                        <t t-esc="rangeKey"/>
                    </td>
                    <td>
                        <t t-esc="formatPayloadValue(value)"/>
                    </td>
                </tr>
            </tbody>
        </table>
    </t>

    <!-- ACTIONS.Payload.vRangeLocation -->
    <t t-name="payloadVRange">
        <table>
            <tbody>
                <tr>
                    <td>
                        direction
                    </td>
                    <td>
                        <t t-esc="value.direction or 'unset'"/>
                    </td>
                </tr>
                <tr>
                    <td>
                        start
                    </td>
                    <td>
                        <t t-esc="value.startPosition"/> <t t-esc="value.start.id"/> (<t t-esc="value.start.value or value.start.type"/>)
                    </td>
                </tr>
                <tr>
                    <td>
                        end
                    </td>
                    <td>
                        <t t-esc="value.endPosition"/> <t t-esc="value.end.id"/> (<t t-esc="value.end.value or value.end.type"/>)
                    </td>
                </tr>
            </tbody>
        </table>
    </t>

    <!-- ACTIONS.Handlers -->
    <t t-name="actionHandlers">
        <div class="divider" t-if="selectedRecord !== False">🎬 Action Handlers</div>
        <table t-if="selectedRecord !== False">
            <tbody>
                <tr t-foreach="Object.keys(selectedRecord)"
                    t-as="handlerToken" t-key="handlerToken_index"
                    class="selectable-line" t-on-click="selectHandler(handlerToken)"
                    t-att-class="{
                        selected: state.selectedHandlerToken == handlerToken,
                    }">
                    <t t-set="handler" t-value="selectedRecord[handlerToken]"/>
                    <td><t t-esc="handlerToken"/></td>
                    <td><t t-esc="handler.name"/></td>
                </tr>
            </tbody>
        </table>
    </t>

    <!-- ACTIONS.SelectedHandler -->
    <t t-name="selectedActionHandler">
        <div t-if="state.selectedHandlerToken" class="divider">🎯 Selected Handler</div>
        <table t-if="state.selectedHandlerToken">
            <t t-set="handler" t-value="getHandler(state.selectedHandlerToken)"/>
            <tbody>
                <tr>
                    <td>token</td>
                    <td><t t-esc="state.selectedHandlerToken"/></td>
                </tr>
                <tr>
                    <td>name</td>
                    <td><t t-esc="handler.name"/></td>
                </tr>
                <tr t-if="!handler.name.startsWith('bound ')">
                    <td>code</td>
                    <td><t t-esc="handler"/></td>
                </tr>
            </tbody>
        </table>
    </t>

    <!-- MAIN -->
    <jw-devtools t-name="devtools"
        t-att-class="{
            closed: state.closed,
        }"
        t-attf-style="height: {{state.height}}px"
        tabindex="2">
        <devtools-navbar
            t-on-click="toggleClosed"
            t-on-mousedown="startResize">
            <button t-on-click="openTab('inspector')" t-att-class="{
                selected: state.currentTab == 'inspector',
            }">Inspector</button>
            <button t-on-click="openTab('actions')" t-att-class="{
                selected: state.currentTab == 'actions',
            }">Actions</button>
        </devtools-navbar>
        <t t-if="!state.closed">
            <InspectorComponent isOpen="state.currentTab == 'inspector'"/>
            <ActionsComponent isOpen="state.currentTab == 'actions'"
                t-ref="ActionsComponent"/>
        </t>
    </jw-devtools>

</templates>
